command: >
  bash -c "
    echo '🚧 Starte bench new-site';
    bench new-site --no-mariadb-socket --admin-password ${ADMIN_PASSWORD} ${SITE_NAME} 2>&1 | tee /tmp/site-setup.log;
    echo '✅ Fertig. Schlafe...';
    sleep 300"

